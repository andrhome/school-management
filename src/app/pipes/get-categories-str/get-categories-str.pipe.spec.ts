import { GetCategoriesStrPipe } from './get-categories-str.pipe';

describe('GetCategoriesStrPipe', () => {
  it('create an instance', () => {
    const pipe = new GetCategoriesStrPipe();
    expect(pipe).toBeTruthy();
  });
});
